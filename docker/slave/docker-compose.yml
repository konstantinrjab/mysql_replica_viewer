version: '3.1'

services:
  rlc_mysql_slave:
    container_name: rlc_mysql_slave
    image: mysql:8
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: 'root'
      MYSQL_DATABASE: 'db'
      MYSQL_USER: 'user'
      MYSQL_PASSWORD: 'password'
    ports:
      - '3306:3306'
    volumes:
      - rlc_mysql_slave:/var/lib/mysql
      - ./mysql/slave:/etc/mysql/conf.d

volumes:
  rlc_mysql_slave:
